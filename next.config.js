/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    PUBLIC_URL: '/',
  },
  // i18n: {
  //   locales: ['en', 'ru'],
  //   defaultLocale: 'en',
  // },
};

module.exports = nextConfig;

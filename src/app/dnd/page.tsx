import { DrugBox } from '@/components/common';
import { FC } from 'react';

interface Props {}
const DnD: FC = (props: Props) => {
  return (
    <div>
      <DrugBox />
    </div>
  );
};
export default DnD;
